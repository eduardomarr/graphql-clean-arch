import { {{pascal entity}} } from '../entities/{{pascal entity}}';

export interface {{pascal entity}}Repository {
  save(item: {{pascal entity}}): Promise<void>;
  findById(id: string): Promise<{{pascal entity}} | null>;
}
