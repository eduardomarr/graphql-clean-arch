import { {{pascal entity}}Repository } from '../../domain/repositories/{{pascal entity}}Repository';
import { {{pascal entity}} } from '../../domain/entities/{{pascal entity}}';

export class InMemory{{pascal entity}}Repo implements {{pascal entity}}Repository {
  private store = new Map<string, {{pascal entity}}>();

  async save(item: {{pascal entity}}) {
    this.store.set(item.id, item);
  }

  async findById(id: string) {
    return this.store.get(id) || null;
  }
}
