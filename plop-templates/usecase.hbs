import { {{pascal entity}} } from '../../domain/entities/{{pascal entity}}';
import { I{{pascal entity}}Repository } from '../../domain/repositories/I{{pascal entity}}Repository';

export class Create{{pascal entity}} {
  constructor(private repo: I{{pascal entity}}Repository) {}

  async execute(input: Partial<{{pascal entity}}>) {
    const item = new {{pascal entity}}(/* preencha attrs */);
    await this.repo.save(item);
    return item;
  }
}
