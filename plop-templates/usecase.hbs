import { {{pascal entity}} } from '../../domain/entities/{{pascal entity}}';
import { {{pascal entity}}Repository } from '../../domain/repositories/{{pascal entity}}Repository';

export class Create{{pascal entity}} {
  constructor(private repo: {{pascal entity}}Repository) {}

  async execute(input: Partial<{{pascal entity}}>) {
    const item = new {{pascal entity}}(/* fill attrs */);
    await this.repo.save(item);
    return item;
  }
}
